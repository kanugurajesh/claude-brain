---
description: Ask the wiki a question; answers cite pages and flag drafts, gaps and staleness
argument-hint: <question>
---

Run **Query** as defined in CLAUDE.md §7. Question: `$ARGUMENTS`

Read `index.md` first, then the relevant pages. Answer concisely with `[[links]]` as citations. Say clearly if the answer is missing, only in a `draft`, marked `[!gap]`/`[!conflict]`, or past its review date. Do not fill gaps from general knowledge without labelling it. Offer to file valuable answers under `knowledge/syntheses/`.
