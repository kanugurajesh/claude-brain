---
description: One-time onboarding interview that fills in the company/GTM profile and seeds the wiki
---

Run **Setup** as defined in CLAUDE.md §7.

Interview me **one question at a time** (short, conversational). Cover: company and product; who the target customer is; current channels and sales motion; team structure (roles, not names); main tools; what is measured today; the biggest open GTM questions. Stop after ~8–10 questions.

Then: fill `knowledge/company.md` (remove `<!-- UNSET -->` markers), create draft stub pages for each tool and concept mentioned (with `[!gap]` callouts), update `index.md`, append a `setup` entry to `log.md`, and recommend the first 3 research briefs or experiments.

$ARGUMENTS
