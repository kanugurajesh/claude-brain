---
description: Weekly human-in-the-loop pass — approve drafts, close experiments, refresh stale research, resolve conflicts
---

Run **Review** as defined in CLAUDE.md §7.

Start with a compact dashboard: drafts awaiting approval, experiments past `end` or concluded without a decision, stale research, SOPs overdue or due within 14 days, unresolved `[!conflict]` items, `[!gap]` count per page, inbox size. Then walk through items one at a time, oldest/most important first, asking for my decision. Only I can approve `active` or start an experiment. Apply approved changes (status, version, dates, Changelog/Decision log), update `index.md`, append a `review` entry to `log.md`.
