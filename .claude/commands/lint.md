---
description: Health-check the wiki for broken links, drift, staleness, gaps and contradictions
---

Run **Lint** as defined in CLAUDE.md §7. Check every wiki page against the Errors / Warnings / Suggestions lists. Fix mechanical problems (broken links, index drift, missing frontmatter) directly; ask before changing meaning. Report grouped by severity with `[[links]]`, then append a `lint` entry to `log.md`.
