# Brain — Log

Append-only. Never edit past entries. Newest at the bottom.
Recent entries: `grep "^## \[" log.md | tail -5`

## [2026-09-20] schema | Reset and retarget Brain to GTM & Ops
- Decisions: at the human's explicit request, erased the previous agency wiki (Skanda Labs pages, sample SOP, two raw sources) and the earlier log; a backup copy was made outside the project first. Content folder renamed `wiki/` → `knowledge/`. New page types `experiment` and `research`; policies, services, roles and clients dropped.
- Created: `CLAUDE.md` (rewritten), `README.md` (rewritten), `_templates/experiment.md`, `_templates/research.md`, commands `/research`, `/experiment`, `/weekly-brief`, `/setup` and `/review` rewritten, empty `knowledge/` folders, [[company]] stub, [[index]].
- Open: fill [[company]] via `/setup` or `/research`.
